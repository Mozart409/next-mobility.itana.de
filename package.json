{
  "name": "next-mobility.itana.de",
  "version": "0.2.4",
  "scripts": {
    "dev": "next",
    "build": "next build && next export",
    "start": "next start",
    "lint": "prettier-standard --lint '**/*.{js,css,json}' && yarn run rw",
    "format": "yarn run rw && prettier-standard --format '**/*.{js,css,json}' ",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "rw": "yarn run rustywind --write ."
  },
  "dependencies": {
    "@headlessui/react": "^0.3.1",
    "next": "latest",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scroll-up": "^1.3.7",
    "twind": "^0.15.6"
  },
  "license": "MIT",
  "prettier": {
    "tabWidth": 2,
    "useTabs": false,
    "semi": false,
    "singleQuote": true,
    "trailingComma": "none"
  },
  "devDependencies": {
    "@ijsto/eslint-config": "^3.0.1",
    "cypress": "^6.6.0",
    "prettier-standard": "^16.4.1",
    "rustywind": "^0.8.0"
  }
}
